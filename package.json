{
  "name": "sole-voltaje",
  "version": "1.0.0",
  "description": "SOLE Voltaje - Sistema de documentación con Quartz",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up -d && echo '✅ Servidor en http://localhost:8080'",
    "dev:watch": "docker compose -f docker-compose.dev.yml up",
    "stop": "docker compose -f docker-compose.dev.yml down",
    "logs": "docker compose -f docker-compose.dev.yml logs -f --tail=50",
    "shell": "docker compose -f docker-compose.dev.yml exec voltaje-dev sh",
    "build": "docker compose -f docker-compose.dev.yml build",
    "clean": "docker compose -f docker-compose.dev.yml down -v",
    "setup": "cd quartz && npm install"
  },
  "keywords": ["quartz", "documentation", "sole", "voltaje"],
  "author": "SOLE Colombia",
  "license": "MIT"
}

